<%= form_for(@superteam) do |f| %>
  <% if @superteam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@superteam.errors.count, "error") %> prohibited this superteam from being saved:</h2>

      <ul>
      <% @superteam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
