<div class="section group">
   <div class="col span_2_of_12">
  </div>
  <div class="col span_8_of_12">
   <img src=<%= @company.image_url %>>
   <%= link_to "edit", "/companies/#{@company.id}/edit" %>
  </div>
</div>




 <div class="section group table">
    <div class="table-row">
    <% @company.products.each do |product| %>
        <div class="col span_1_of_12 product_line">
          <%= link_to "/products/#{product.id}/edit" do %>
          <img src=<%= product.image_url %>>
          <% end %>
        </div>
    <% end %>

      <div class="col span_1_of_12 product_line">
        <%= link_to "/products/#{@company.id}/new/" do %>
        <h1 class="add">+<h1>
        <% end %>
      </div>
    </div>
  </div>


<%= link_to "Add new product", "/products/#{@company.id}/new/" %>

