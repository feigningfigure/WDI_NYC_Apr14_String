
# janes_favorite_colors | omars_favorite_colors
# merges two arrays


janes_favorite_colors = ["red", "blue", "green" ]
omars_favorite_colors = ["yellow", "purple", "red" ]

def merge_array





def merge_two_arrays(item, item2)
merge_array = []
  array1.each do |item|
    array1.each do |item2|
      if item != item2
        merge_array << item2
      end
    end
  end
  return merge_array
end

def array_plus(array1, array2)
  array3 = array1 + array2
  arra3.flatten.unique
end


