function Hangman (word) {

  var playerProgress = [];
//for each letter in the string....
  for (var i = 0; i < word.length; i++) {
//add an underscore to the array
    playerProgress.push("");
  } //string => array
  return playProgress.join("") // string => array
};
}

