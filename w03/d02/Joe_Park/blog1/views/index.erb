<ul class="posts">
	<% @posts.each do |post| %>
		<li>
			<h2><%= post["title"] %></h2>
			<p><%= post["body"] %></p>
			<p>
				by <%= post["created_at"] %> on <%= post["author"] %>
			</p>
		</li>
	<% end %>
</ul>